---
description: 代码标识符、路径与资源命名禁止使用汉语拼音；使用有意义的英文。
alwaysApply: true
---

# 命名：禁止拼音

- **适用范围**：`miniprogram/`、`cloudfunctions/` 及与本仓库相关的脚本中的 **标识符**（变量、函数、类型、常量、组件/类名、事件名、Storage key、云字段名等）、**目录名**、**文件名**、**分包 root/name**、**路由 path**、**tabBar 图标文件名**（当作为资源路径参与代码时）。
- **要求的风格**：使用 **有意义的英文**（可为一个完整英文词或常见复合词，如 `moments`、`resonance`、`reportFilter`），遵循项目现有命名习惯；避免音译拼音（如 `fusheng`、`gongming`）。
- **例外**：面向用户的 **UI 文案**（WXML 中文、`navigationBarTitleText`、`text` 等）可使用中文。
- **持久化键名**：若云库或客户端**已有**非拼音旧字段需兼容，新写入统一用英文键；**不要**为从未落库的中间命名（如昙花一现的拼音键）保留兼容分支或字面量——确认无数据后直接删。
