---
description: 项目官方五色配色体系；页面与 UI 设计必须遵守
alwaysApply: true
---

# 项目配色体系（官方色板）

实现或设计小程序/页面 UI（含 WXSS、导航栏 `navigationBarBackgroundColor`、组件配色、图标主色）时，**主视觉必须基于下列五色**，通过透明度、渐变（仅限这些色相之间的混合）与中性灰白扩展层次；不要随意引入与整套色相冲突的高饱和色（如默认 Material 蓝、亮紫、荧光色）作为主色。

## 五色定义（唯一来源）

| 用途建议 | 名称 | Hex |
|----------|------|-----|
| 强调、次要按钮、标签点缀 | 灰粉 Dusty Rose | `#D6A2AD` |
| 大面积背景、卡片底、分区 | 沙米 Sandy Beige | `#C3B59F` |
| 正向提示、自然感辅助图形 | 鼠尾草绿 Sage | `#A0AF84` |
| 主操作、链接、选中态 | 青绿 Muted Teal | `#668F80` |
| 标题与正文深色、重对比元素 | 深灰蓝 Slate | `#4A6670` |

## 使用原则

- **主交互**：主按钮、关键链接、选中态优先 `#668F80`；需更强对比的文案或图标用 `#4A6670`。
- **背景**：页面/区块背景以 `#C3B59F` 或在该色相上叠 `rgba(255,255,255,…)` / `rgba(74,102,112,…)` 分层，避免突兀纯色块。
- **点缀**：次要标签、徽章、图标点缀用 `#D6A2AD` 或 `#A0AF84`，保持柔和统一。
- **无障碍**：在浅色底上保证文字与背景对比度可读；必要时允许 `#ffffff` / `#000000` 仅用于文字或小面积描边，不改变整体色相走向。

## WXSS 参考

```css
/* 主按钮 */
.btn-primary { background: #668F80; color: #ffffff; }
/* 页面背景 */
.page { background: #C3B59F; }
/* 标题 */
.title { color: #4A6670; }
/* 点缀 */
.accent { color: #D6A2AD; }
.positive { color: #A0AF84; }
```
